name: agentp2p-dev

services:
  # Generated by develop/scripts/gen_envs.py
  # Keep this file as a human-readable template; use docker-compose.generated.yml to run.
  # Optional: add lobsterN services via --with-lobster
