FROM python:3.12-slim-bookworm

WORKDIR /app

RUN pip install --no-cache-dir websockets requests \
    || pip install --no-cache-dir -i https://mirrors.aliyun.com/pypi/simple/ --trusted-host mirrors.aliyun.com websockets requests

COPY . /app

CMD ["python", "develop/local/bridge_dev.py"]
